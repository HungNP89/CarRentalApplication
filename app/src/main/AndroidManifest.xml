<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarRentalApplication"
        tools:targetApi="tiramisu">
        <activity
            android:name=".uiActivity.NormalCar"
            android:exported="false" />
        <activity
            android:name=".uiActivity.Category"
            android:exported="false" />
        <activity
            android:name=".uiActivity.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".uiActivity.Register"
            android:exported="false" />
        <activity
            android:name=".uiActivity.Login"
            android:exported="false" />
        <activity
            android:name=".uiActivity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".uiActivity.SplashScreen"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>